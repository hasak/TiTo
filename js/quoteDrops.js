$(".quoteDropdownItem").click(function () {    var option = $(this).attr("data-option");    for (var i = 0; i < $(".quoteOptionWrap").length; i++) {        var buh = $(".quoteOptionWrap").eq(i).attr("data-option");        if (buh == option) {            $(".quoteOptionWrap").slideUp();            $(".quoteOptionWrap").eq(i).slideDown();            $("#quoteDropdownButton").html($(this).html()).attr("data-koji", buh);        }    }    $("#saveQuoteBtn").slideDown();});$(".callItem").click(function () {        $("#alertDropdownButton").html($(this).find(".zahtm").html()).attr("data-kojical", $(this).attr("data-option"));    });